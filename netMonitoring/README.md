# netMonitoring README
Required packages include the python termcolor
```
sudo pip install termcolor
```
and in order to make the ping work on the pi...need to set permissions:
```
sudo chmod u+s /bin/ping
```
